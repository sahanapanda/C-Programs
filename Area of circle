#include <stdio.h>

int main() {
    float pi;
    float radius, area;
    
    pi = 3.14;
    
    printf("enter radius of circle :");
    scanf("%f", &radius);
    
    area = pi*radius*radius;
    
    printf("Area of circle is : %.2f", area);

    return 0;
}
